import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meethybrid.habittracker',
  appName: 'Habit Tracker',
webDir: 'out',

};

export default config;
